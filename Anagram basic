Given two strings A and B consisting of lowercase characters. Print TRUE if A and B are anagrams otherwise FALSE.

Input
smartinterviews
viewsintersmart
Output
TRUE

======
s1=input()
s2=input()
l1=list(s1)
l2=list(s2)
l1.sort()
l2.sort()
for i in range(len(l1)):
    if l1[i]==l2[i]:
        if i==len(l1)-1:
            print("TRUE")
    else:
        print("FALSE")
        break
